# cozy-smile / フロント静的サイト用
# Railway → サービス Settings → Config-as-code → Railway Config File: railway.frontend.toml
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.railway-frontend"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
